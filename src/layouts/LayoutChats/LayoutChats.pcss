.chats {
    display: flex;
    height: 100vh;

    &__nav {
        width: 310px;
        background: var(--color-background-secondary);
        border-right: 1px solid var(--color-border);
    }

    &__content {
        flex-grow: 1;
    }
}
